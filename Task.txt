Deploy and host the application: Extend your previous secure group-chat application and deploy its backend on at least 3 allotted systems. Host the application through a Load Balancer, so that clients access only the Load Balancer URL/port.
Implement Dynamic Load Balancing: The Load Balancer must dynamically select a backend using response time and/or current system load,rather than simple fixed Round-Robin routing.  It must monitor backend health and stop sending requests to an unavailable backend.
Demonstrate Dynamic Scaling: Start the system with one backend, generate increasing load, and then initialize additional backend systems while the application is running. The Load Balancer should automatically detect the new backend and start distributing traffic to it. Explain and show how the system performance changes after each backend is added.
Ensure Database Persistence and No Duplicates: All backend servers must use persistent storage and share the same chat data. Every message must have a unique message ID, and your database/application must prevent duplicate insertion when the same message is received multiple times, such as during retries or reconnections.
Perform Experiments and Analysis: Generate increasing load from your local system and record response time, throughput, successful/failed requests, and number of active backends. Your report must include at least a Response Time vs Load graph , throughput vs offered load aand a graph showing the effect of adding new backend servers, along with comparison tables and analysis.
Submission and Demonstration: This is an individual assignment. Submit a single PDF report containing student details, assigned systems and ports, architecture, Load Balancer design, database/duplicate-prevention approach, graphs,comparison tables, relevant screenshots, and source-code/GitHub link. Demonstrate dynamic backend addition, persistence, duplicate prevention, and backend failure/recovery on the allotted systems.

//Updated Task
Deploy and host the application: Extend your previous secure group-chat application and deploy its backend on the 3 allotted systems. Host the application through a Load Balancer, so clients access only the Load Balancer URL/port. Submit the Load Balancer URL; the load generator will be run from our side.
Implement Performance-Based Dynamic Load Balancing: The Load Balancer must dynamically select backends based on system performance/load. When the load of the current backend exceeds a defined threshold, traffic must switch to another suitable backend. Fixed Round-Robin alone is not acceptable. The Load Balancer must also detect unhealthy/unavailable backends and stop routing requests to them.
Optimize the Threshold: Determine an optimal performance threshold for switching between backends. The leaderboard will evaluate all submitted URLs using the load generator and will be updated only at the end of the evaluation period.
Ensure Database Persistence and No Duplicates: All backend servers must use persistent storage and share the same chat data. Every message must have a unique message ID, and the application/database must prevent duplicate insertion when a message with the same ID is received multiple times due to retries, reconnections, or failures.
Required API Routes: The Load Balancer must expose the following exact routes:
/message — accepts "client-name" and "msg" as input and submits a message.
/feed — retrieves all messages.
Students may implement the chatroom/group/place and internal routing of messages in any way they choose, but these two routes and their exact URL paths must be maintained. The submission link will be updated accordingly to use these routes.Load Generation and Evaluation: An official load generator will be operated from our side using the specified API routes. Students must also develop their own load generator for reporting and experimentation purposes. The load generator should support a variable number of users, random/variable message lengths, and random/variable time intervals between messages.Report and Submission: This is an individual assignment. Submit the Load Balancer URL/endpoint and a report containing the code/repository details. The report must include plots generated using your own load generator, including response time and system utilization of all 4 systems. The submitted application must be based on the previous secure, persistent group-chat application and must not be simplified or have existing functionality removed solely to achieve a higher leaderboard rank. Such modifications will result in deduction of marks.